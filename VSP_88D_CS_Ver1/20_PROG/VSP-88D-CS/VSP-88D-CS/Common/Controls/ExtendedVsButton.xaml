<UserControl
    x:Class="VSP_88D_CS.Common.Controls.ExtendedVsButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:VSControls="clr-namespace:VSLibrary.UIComponent.VSControls;assembly=VSLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:VSP_88D_CS.Common.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <VSControls:VsButton
        Height="{Binding ButtonHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Background="#FFE6E5E5"
        Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
        CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}"
        FontSize="15"
        Foreground="{Binding ButtonForeground, RelativeSource={RelativeSource AncestorType=UserControl}}">

        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <Image
                Margin="5"
                Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Stretch="Fill"
                Visibility="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}" />
            <TextBlock
                VerticalAlignment="Center"
                LineStackingStrategy="BlockLineHeight"
                Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
    </VSControls:VsButton>
</UserControl>
