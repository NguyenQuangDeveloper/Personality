<UserControl
    x:Class="AlarmConfig.Views.Tops.ZoomTool"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AlarmConfig.Views.Tops"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="150"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <Grid>
        <Border BorderBrush="LightGray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="70" />
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button
                        Grid.Column="0"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Command="{Binding ZoomInCommand}">
                        <Image
                            Margin="4"
                            Source="pack://application:,,,/AlarmConfig;component/Resources/Images/zoomin.png"
                            Stretch="Uniform" />
                    </Button>
                    <Button
                        Grid.Column="1"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Command="{Binding ZoomOutCommand}">
                        <Image
                            Margin="4"
                            Source="pack://application:,,,/AlarmConfig;component/Resources/Images/zoomout.png"
                            Stretch="Uniform" />
                    </Button>
                    <Button
                        Grid.Column="2"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Command="{Binding ZoomResetCommand}">
                        <Image
                            Margin="4"
                            Source="pack://application:,,,/AlarmConfig;component/Resources/Images/fullScreen.png"
                            Stretch="Uniform" />
                    </Button>
                </Grid>
                <Label
                    Grid.Row="2"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Bottom"
                    Content="{Binding LanguageResources[Zoom], FallbackValue='Zoom'}"
                    FontWeight="DemiBold"
                    Foreground="Gray" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
